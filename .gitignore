# ─── Yerel docker-compose testi için gerekli ama Railway'i kırdığı için ────
# ASLA commit edilmemeli (Railway Nixpacks kullanıyor, Dockerfile görünce
# otomatik ona geçip build'i kırıyor — bkz. "nest-cli.json bulunamadı" hatası)
apps/api/Dockerfile

# ─── Ortam değişkenleri (GİZLİ BİLGİLER — asla commit edilmemeli) ───────────
.env
.env.local
.env.*.local
apps/api/.env
apps/web/.env
apps/web/.env.local

# ─── Bağımlılıklar ───────────────────────────────────────────────────────────
node_modules/
**/node_modules/

# ─── Derleme çıktıları ───────────────────────────────────────────────────────
apps/web/.next/
apps/api/dist/
*.tsbuildinfo

# ─── Yüklenen dosyalar (kalıcı depolama, git'e girmemeli) ────────────────────
apps/api/uploads/
apps/api/apps/api/uploads/

# ─── Log ve önbellek ─────────────────────────────────────────────────────────
*.log
npm-debug.log*
.cache/

# ─── İşletim sistemi / editör ────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
